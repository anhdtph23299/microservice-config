server:
  port: 9000

message: "Hello, this is the Gateway configuration from GitHub!"

# Thêm một vài cấu hình khác để test
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
